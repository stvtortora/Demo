export const TabSectionHeaderProps = (theme) => ({
  color: theme.text.colors.secondary,
  style: theme.text.styles.primary,
  size: theme.text.sizes.xlarge,
  margin: "26px 0",
});

export const GridElementHeaderProps = (theme) => ({
  color: theme.text.colors.primary,
  style: theme.text.styles.primary,
  size: theme.text.sizes.large,
  margin: "12px 10px 0",
});
